package recruitment.dev.ragservice.dto;

/** The type of HR resource the assistant is allowed to select for a filter. */
public enum HrAssistantScope {
    APPLICATIONS,
    CANDIDATES,
    JOB_OFFERS
}
